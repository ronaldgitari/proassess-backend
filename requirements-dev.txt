# Test / dev dependencies (install into the API container or a local venv to run tests):
#   docker compose exec api pip install -r requirements-dev.txt
#   docker compose exec api pytest
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.1
